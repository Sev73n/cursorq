- Tauri 2 (Rust) 结合 Vite (TypeScript) 构建跨平台桌面应用。
- 使用 `sql.js` (WASM) 在 Node.js 环境中解析 Cursor 的 `state.vscdb` SQLite 数据库以提取认证 Token。
- 集成 `reqwest` (Rust) 进行远程内容同步，利用 Windows API (`windows` crate) 实现无边框窗口的 DWM 阴影与激活状态管理。