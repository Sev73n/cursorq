- 开发需配置 Rust MSVC 工具链与 Node.js 20+，通过 `npm run dev` 或 `scripts/dev-tauri.sh` 启动。
- 打包使用 `npm run package:win` 生成包含便携运行时的 ZIP 包，支持在无 Node/Rust 环境的 Windows 上运行。
- 提供 `scripts/slice-mascot-sprites.py` 用于处理吉祥物精灵图，以及 `scripts/validate-copy.mjs` 校验文案格式。